---
import Appstores from '../../components/Appstores';
import FeatureImage from '../../components/FeatureImage';
import Gallery from '../../components/Gallery';
import SalesPitch from '../../text/salesPitch_FR.mdx';
import Layout_Body from '../../layouts/Layout_Body.astro';
import type { Language } from '../../types/types_global';
import { claude_prompt } from "../../utils/claude_prompt";


const language = Astro.currentLocale as Language

let reviewTranslations = null
try{
	reviewTranslations = await claude_prompt(language);
}catch(e){
	console.error(e)
}
---

<Layout_Body language={language} >
	
	<FeatureImage language={language} />

	<Appstores language={language} />

	<section class="content" id="home">
		<SalesPitch reviewTranslations={reviewTranslations} language={language}/>
	</section>

	<Gallery language={language} />

</Layout_Body>
